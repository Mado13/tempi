{
  "name": "@tempi/mobile",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "CAP_DEV=true bunx cap run android",
    "ios": "CAP_DEV=true bunx cap run ios",
    "sync": "bunx cap sync",
    "build:android": "bunx cap build android",
    "build:ios": "bunx cap build ios"
  }
}
